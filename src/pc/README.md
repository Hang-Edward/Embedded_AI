# PC C++ Application

这里用于放置后续 PC 端 C++ 主程序。

计划职责：

- 调用 OpenCV 读取 Logitech C270 摄像头画面。
- 接收语音输入或按键触发事件。
- 调用千问视觉/多模态 API。
- 解析模型返回的结构化结果。
- 通过串口向 NUCLEO-F446RE 发送 LED、蜂鸣器、OLED、震动等反馈命令。
- 记录审计日志，并实现课程要求中的随机文件读写更新。

当前阶段先保留目录说明，等硬件模块和接口协议稳定后再正式加入 C++ 源码。
